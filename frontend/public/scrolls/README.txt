Scroll/crow sprite + image assets live here.
Filenames are configured in /admin → Scrolls (scrolls_settings / scrolls_frames).
Defaults expected by the seed config:
  scroll_blank.png            - blank scroll background
  seal_open.png               - wax seal (unstamped) = send button
  seal_stamped.png            - wax seal (stamped) on send
  branch_send.png             - fixed branch, left edge (send sequence)
  branch_land.png             - fixed branch, right edge (landing sequence)
  crow_send_00.png .. _11.png - send/flight sequence frames (12)
  crow_land_00.png .. _11.png - landing sequence frames (12)
Until present, the UI shows visible placeholders so logic is testable.
